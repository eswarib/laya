# client

Minimal interactive MCP client.

## Run

From `mcp-agent-os/`:

```bash
npm -w client run dev
```

It reads `mcp.json`, spawns servers over stdio, runs `tools/list`, and lets you call tools.




